package com.lek.spaceimpact.game.domain

import com.lek.spaceimpact.R

enum class LongMusic(val resource: Int) {
    MENU(R.raw.menu),
    HOME(R.raw.home),
    LEVEL_ONE(R.raw.in_game),
    ABOUT_GAME(R.raw.about_game)
}