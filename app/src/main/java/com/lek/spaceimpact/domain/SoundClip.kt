package com.lek.spaceimpact.domain

import com.lek.spaceimpact.R

enum class SoundClip(val resource: Int) {
    GUN_FIRE(R.raw.gun_fire),
    SUCCESS(R.raw.success),
    ENEMY_JET_FIRED(R.raw.jet_fired)
}